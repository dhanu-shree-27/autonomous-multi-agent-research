# Copy this file to `.env` and fill in real values.
# NEVER commit the real `.env` file — it's already excluded in .gitignore.

# --- App settings ---
APP_NAME=Autonomous Multi-Agent Research System
APP_ENV=development
DEBUG=true
LOG_LEVEL=INFO

# --- OpenAI (required from Phase 2 onward, validated but unused in Phase 1) ---
OPENAI_API_KEY=your-openai-api-key-here
