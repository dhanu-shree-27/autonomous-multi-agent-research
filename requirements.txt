# Phase 1 dependencies only — kept minimal on purpose.
# Agent SDK, search tools, PDF generation, and DB drivers are added in later phases.

fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
pytest==8.3.3
httpx==0.27.2
